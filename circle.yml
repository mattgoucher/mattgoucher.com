machine:
  node:
    version: 4.2.4

dependencies:
  pre:
    - npm install npm@2.14.12 -g

deployment:
  production:
    branch: master
    commands:
      - ./build/version.sh
    heroku:
      appname: mattgoucher
